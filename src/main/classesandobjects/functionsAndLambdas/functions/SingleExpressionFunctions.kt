package main.classesandobjects.functionsAndLambdas.functions

fun doubleTheValue(x:Int) = x * 2 //--> single expression at the simplest form. Here the return type is omitted and the curly braces are omitted too.